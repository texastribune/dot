DB_HOST=dot-db
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dot
DB_PORT=5432
AWS_ACCESS_KEY_ID=<redacted>
AWS_SECRET_ACCESS_KEY=<redacted>
SENTRY_URL=<redacted>
SENTRY_PUBLIC_URL=<redacted>

DB_*
Variables correspond to a local dev PostgreSQL database running inside Docker.

AWS_*
For downloading a snapshot of the production database for local use.

SENTRY_*
For exception handling via Raven and Sentry.
